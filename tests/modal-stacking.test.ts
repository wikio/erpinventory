import { describe, expect, it } from 'vitest';import { readFileSync } from 'node:fs';import { join } from 'node:path';const source=(file:string)=>readFileSync(join(process.cwd(),file),'utf8');
describe('PV type nested modal stacking',()=>{it('places DialogManager forms above the highest manager overlay',()=>{const dialogs=source('js/dialogs.js'),styles=source('css/styles.css'),helpers=source('js/compliance-helpers.js');expect(helpers).toContain('DialogManager.form');expect(dialogs).toContain("Math.max(200");expect(dialogs).toContain("querySelectorAll('.sari-modal-backdrop,.sari-dialog-backdrop')");expect(styles).toContain('z-index: 200');});});
