import { afterEach, describe, expect, it } from 'vitest';
import { mkdtempSync, readFileSync, rmSync } from 'node:fs';import { tmpdir } from 'node:os';import { join } from 'node:path';import { createRequire } from 'node:module';const require=createRequire(import.meta.url);const{SmtpService}=require('../smtp-service.js');const roots:string[]=[];afterEach(()=>roots.splice(0).forEach(root=>rmSync(root,{recursive:true,force:true})));
describe('SMTP secret handling',()=>{it('persists SMTP metadata without writing the password',()=>{const root=mkdtempSync(join(tmpdir(),'sari-smtp-'));roots.push(root);const service=new SmtpService(root);service.save({host:'smtp.example.test',username:'sari',password:'TopSecret!',enabled:true});const raw=readFileSync(join(root,'.runtime','smtp.json'),'utf8');expect(raw).not.toContain('TopSecret!');expect(service.public()).toMatchObject({host:'smtp.example.test',hasPassword:true});expect(service.public()).not.toHaveProperty('password');});});
